insert into dds.purchase_contract_position
select 
ekpo."ebeln" as purchase_contract_code,
ekpo."ebelp" as position_line_item_code,
ekko."bstyp" as purchase_document_type_code,
ekpo."pstyp" as position_type_code,
ekpo."lmein" as base_uom_code,
ekpo."zzbucking" as booking_number,
ekpo."zzbunker" as bunker_adjustment_tariff_calculation_rule_code,
ekpo."zzlength" as container_length_type_code,
ekpo."zzpr_cont" as container_ownership_type_code,
ekpo."zzstock" as container_pickup_transport_hub_code,
ekpo."umren" as conversion_denominator_to_base_uom,
ekpo."bpumn" as conversion_denominator_to_purchase_contract_uom,
ekpo."umrez" as conversion_numerator_to_base_uom,
ekpo."bpumz" as conversion_numerator_to_purchase_contract_uom,
ekpo."zzkontrag" as counterparty_code,
ekpo."zzland" as country_of_origin_code,
ekpo."bstae" as delivery_confirmation_control_code,
ekpo."zztariff" as dob_tariff_responsibility_code,
ekpo."prdat" as dt_price_calculated,
ekpo."zzet_tarif" as etsng_code,
ekpo."zzetsng1" as etsng_previous_code,
ekpo."knttp" as expense_assignment_type_code,
ekpo."zzrej_exp" as export_method_code,
ekpo."fipos" as financial_position_code,
ekpo."zzfraht_type" as freight_movement_method_code,
ekpo."zzgrade" as fuel_grade_code,
ekpo."geber" as fund_code,
ekpo."fistl" as funds_center_code,
ekpo."zztype_prod" as import_method_code,
ekpo."zzinco1" as incoterms_code,
ekpo."zzknote" as incoterms_location_code,
case when ekpo."loekz" is not null then 'X' else null end as is_deleted,
ekpo."elikz" as is_delivery_completed_code,
ekpo."zzrnpt" as is_material_rnpt_code,
ekpo."zztamozhsous" as is_purchase_customs_union_eeu_code,
ekpo."zzintmarket" as is_purchase_domestic_market_code,
ekpo."zzimport" as is_purchase_export_market_code,
ekpo."zzundefined" as is_purchase_undefined_code,
ekpo."matnr" as material_code,
ekpo."matkl" as material_group_code,
ekpo."ematn" as material_main_code,
ekpo."zzkod_okpd" as material_okpd2_code,
ekpo."zzmatkl" as material_reporting_group_code,
ekpo."zzspart" as material_reporting_type_code,
ekpo."mtart" as material_type_code,
ekpo."zzmovement" as movement_scheme_code,
ekpo."konnr" as number_of_principal_purchase_agreement,
ekpo."zzoption" as option_to_buy_percentage,
ekpo."zztaresubtype" as package_subtype_code,
ekpo."zzcartare" as package_type_code,
ekpo."zzihrez" as paydox_registration_number,
ekpo."werks" as plant_code,
ekpo."zzpr_pl" as platform_ownership_type_code,
ekpo."zzport" as port_code,
ekpo."txz01" as position_name,
ekpo."peinh" as price_unit_code,
ekpo."bprme" as price_uom_code,
ekpo."netpr" as price_vat_excluded_document_currency_amount,
ekpo."mfrnr" as producer_code,
ekpo."ktpnr" as purchase_contract_position_code,
ekpo."banfn" as purchase_requisition_code,
ekpo."bnfpo" as purchase_requisition_position_code,
ekpo."menge" as quantity,
ekpo."zzlif_owner" as railcar_owner_code,
ekpo."zzset" as railway_platform_completeness_type_code,
ekpo."zzstation" as railway_station_code,
ekpo."zzwerks" as receiving_plant_code,
ekpo."zzredir_type" as redirection_type_code,
ekpo."zzroute" as route_code,
ekpo."zzline" as sea_forwarder_code,
ekpo."zzroute2" as sea_route_code,
ekpo."zzsrvpos" as service_code,
ekpo."zztype" as service_type_code,
ekpo."zzforma" as shape_code,
ekpo."zzvsart" as shipment_type_code,
ekpo."ekkol" as supplier_transportation_requirement_group_code,
ekpo."zzformula" as tariff_calculation_rule_code,
ekpo."zzdayoff" as tariff_calculation_weekday_type_code,
ekpo."zzcargow" as tariff_calculation_weight_from,
ekpo."zzcargoa" as tariff_calculation_weight_to,
ekpo."zzterminal" as terminal_code,
ekpo."zzthc" as terminal_handling_charges_payer_code,
ekpo."zzkod_tnved" as tnved_code,
ekpo."zztolerance" as tolerance_percentage,
ekpo."ktmng" as total_contract_limit_quantity,
ekpo."zztrnakl" as transport_bill_code,
ekpo."zzsdabw2" as transport_previous_type_code,
ekpo."zztraty" as transport_subtype_code,
ekpo."zzsdabw" as transport_type_code,
ekpo."meins" as uom_code,
ekpo."mwskz" as vat_code,
ekpo."netwr" as vat_excluded_document_currency_amount,
ekpo."zzvehicletype" as vehicle_detail_type_code,
ekpo."zzbodylength" as vehicle_length_type_code,
ekpo."zzvehicle" as vessel_code,
ekpo."zzkran" as vessel_crane_code,
ekpo."zzbasis" as vessel_port_call_code,
ekpo."lgort" as warehouse_code,
ekpo."gewei" as weight_uom_code
from ods."ekpo_ral" as ekpo
left join ods.ekko_ral as ekko on ekpo.ebeln = ekko.ebeln
where ekko.bstyp = 'K';